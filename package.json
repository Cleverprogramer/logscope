{
  "name": "logscope",
  "version": "0.1.0",
  "private": true,
  "description": "Terminal-based log analysis & observability toolkit",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "dev": "bun run packages/cli/src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
